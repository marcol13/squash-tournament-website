type CardType = {
  title: string;
  image?: string;
  date: string;
  registered: number;
  maxRegistered: number;
  minAge: number;
  maxAge: number;
  price?: number;
  link: string;
};

export default CardType;
